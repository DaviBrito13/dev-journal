<h1>Dev Journal</h1>
<hr class="my-8">
<% @blog_posts.each do |blog_post| %>
  <div>             <!- Texto        path -!>
    <h2><%= link_to(blog_post.title, blog_post_path(blog_post.id), class:"no-underline hover:underline") %></h2>
    <p><%= blog_post.body %></p>
  </div>
<%end%>
